import sys
print(sys.argv)
# def sum():
  #  print(int(sys.argv[1])+int(sys.argv[2]))
# sum()
print(sys.version)
# InterPreter will search for Package in theses Path
print(sys.path) 
print(sys.maxsize) # it returns the maximum size of the data structure that the system can hold. it can varies from system to system
print(sys.stdin)  # stdin and stdout are file system objects which are used for input and output data
print(type(sys.stdout))